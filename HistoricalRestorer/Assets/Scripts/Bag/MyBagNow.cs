using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class MyBagNow : MonoBehaviour
{
    public List<BagItem> info = new List<BagItem>();
    //我要怎么把这里的列表套接到MyBagNowItems里的列表里去呢？
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
       
    }
}
